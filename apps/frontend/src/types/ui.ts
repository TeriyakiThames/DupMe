// GameHeader types
export interface GameHeaderProps {
  // ...add props as needed from the component
}

// GameMode types
export interface GameModeProps {
  // ...add props as needed from the component
}

// GameModeCard types
export interface GameModeCardProps {
  // ...add props as needed from the component
}

// RoomCard types
export interface RoomCardProps {
  initialRoomId?: string;
}
